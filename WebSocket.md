# WebSocket

## WebSocket이란?

WebSocket은 클라이언트와 서버 사이에 **지속적인 양방향 통신 연결**을 만드는 프로토콜이다. 처음에는 HTTP 요청으로 연결을 시작하지만, `Upgrade` 핸드셰이크가 성공하면 같은 연결을 유지하면서 클라이언트와 서버가 서로 원할 때 데이터를 보낼 수 있다.

일반적인 HTTP 통신에서는 클라이언트가 요청해야 서버가 응답한다. 반면 WebSocket에서는 연결이 열린 뒤 서버도 클라이언트의 추가 요청을 기다리지 않고 이벤트를 전송할 수 있으므로 채팅, 알림, 실시간 게임, 협업 편집 등에 적합하다.

### 연결 과정

1. 클라이언트가 `ws://` 또는 암호화된 `wss://` 주소로 접속한다.
2. 클라이언트가 HTTP Upgrade 요청을 보낸다.
3. 서버가 `101 Switching Protocols`로 응답한다.
4. HTTP 연결이 WebSocket 연결로 전환되고, 연결이 닫힐 때까지 양방향 메시지를 주고받는다.

```text
Client                         Server
  | --- HTTP Upgrade 요청 ------> |
  | <--- 101 Switching Protocols- |
  | <==== WebSocket 메시지 =====> |
  | -------- 연결 종료 ---------> |
```

## HTTP와 WebSocket 비교

| 구분 | HTTP | WebSocket |
|---|---|---|
| 통신 방식 | 요청-응답 | 양방향 실시간 통신 |
| 연결 | 보통 요청마다 처리되며 Keep-Alive로 재사용 가능 | 연결을 맺은 뒤 지속적으로 유지 |
| 데이터 전송 주체 | 클라이언트가 요청해야 서버가 응답 | 클라이언트와 서버 모두 전송 가능 |
| 서버의 즉시 알림 | 폴링이나 SSE 등의 별도 방식 필요 | 서버가 즉시 Push 가능 |
| 오버헤드 | 요청마다 HTTP 헤더가 반복될 수 있음 | 연결 후 작은 프레임 단위로 전송 |
| 적합한 상황 | REST API, 페이지 조회, 파일 업로드 | 채팅, 실시간 알림, 게임, 협업 기능 |
| 상태 관리 | 요청 단위로 처리하기 쉬움 | 연결 상태와 접속 사용자 관리 필요 |
| 확장 시 고려사항 | 일반적인 로드 밸런싱 | 장시간 연결, 세션 고정 또는 메시지 브로커 필요 |
| 보안 | HTTPS(`https://`) 사용 | WSS(`wss://`) 사용 |

WebSocket은 HTTP를 대체하는 모든 통신 방식이 아니다. 회원가입, 메시지 목록 조회, 파일 업로드처럼 요청-응답 형태가 자연스러운 기능은 HTTP API로 처리하고, 새 메시지 전달이나 읽음 상태처럼 즉시 반영해야 하는 이벤트는 WebSocket으로 처리하는 방식이 일반적이다.

## 실시간 채팅 서비스의 전체 구조

```text
┌────────────┐       HTTPS API        ┌──────────────┐
│ 웹/모바일   │ ─────────────────────> │ API 서버      │
│ 클라이언트  │                        │ 인증/조회     │
│            │ <───────────────────── │              │
│            │     WSS 실시간 연결    └──────┬───────┘
│            │ ══════════════════════════════│
└────────────┘                              │
                                             │ WebSocket 서버
                                      ┌──────▼───────┐
                                      │ 연결/방 관리  │
                                      └──┬────────┬──┘
                                         │        │
                              ┌──────────▼─┐  ┌──▼──────────┐
                              │ 메시지 브로커│  │ 데이터베이스 │
                              │ Redis 등    │  │ 메시지 저장  │
                              └──────┬─────┘  └─────────────┘
                                     │
                              다른 WebSocket 서버들
```

### 주요 구성 요소

- **클라이언트**: 로그인 후 WebSocket을 연결하고, 메시지를 보내며, 서버 이벤트를 수신한다. 연결이 끊기면 재연결하고 중복 메시지를 방지할 수 있도록 메시지 ID를 관리한다.
- **HTTP API 서버**: 로그인, 토큰 발급, 대화방 목록, 과거 메시지 조회, 파일 업로드 등을 담당한다.
- **WebSocket 서버**: 연결된 소켓, 사용자, 대화방을 관리한다. 수신한 메시지의 인증·권한을 확인한 뒤 저장하고 해당 방의 사용자에게 전달한다.
- **데이터베이스**: 사용자, 대화방, 참여자 권한, 메시지, 읽음 상태를 영구 저장한다.
- **메시지 브로커**: WebSocket 서버가 여러 대일 때 서버 간 메시지를 전달한다. 사용자가 어느 서버에 연결되어 있어도 같은 대화방의 모든 사용자에게 메시지를 보낼 수 있게 한다.
- **로드 밸런서/리버스 프록시**: HTTPS와 WSS 트래픽을 분산한다. WebSocket Upgrade 헤더와 장시간 연결을 지원하도록 설정해야 한다.

## 메시지 처리 흐름

1. 사용자가 HTTP API로 로그인하고 액세스 토큰을 받는다.
2. 클라이언트가 토큰을 이용해 `wss://example.com/ws`에 연결한다.
3. 서버는 토큰을 검증하고 사용자 연결을 등록한다.
4. 클라이언트가 대화방 입장 또는 메시지 전송 이벤트를 JSON으로 보낸다.
5. 서버는 대화방 참여 여부와 메시지 형식을 검증한다.
6. 서버는 메시지를 데이터베이스에 저장하고 고유한 메시지 ID와 생성 시간을 부여한다.
7. 서버는 해당 대화방에 연결된 사용자들에게 `message.created` 이벤트를 전송한다.
8. 클라이언트는 메시지를 화면에 표시하고 필요하면 읽음 이벤트를 서버에 전송한다.

예시 메시지 형식:

```json
{
  "type": "message.send",
  "requestId": "req-123",
  "roomId": "room-1",
  "text": "안녕하세요"
}
```

서버가 보내는 이벤트에는 `type`, `messageId`, `roomId`, `senderId`, `createdAt`처럼 클라이언트가 처리에 필요한 필드를 포함하는 것이 좋다.

## 운영 시 반드시 고려할 점

- **인증과 권한**: 연결 시 토큰을 검증하고, 메시지를 보낼 때마다 해당 방의 참여자인지 확인한다. 클라이언트가 보낸 사용자 ID나 방 권한을 그대로 신뢰하지 않는다.
- **재연결**: 네트워크 단절을 고려해 지수 백오프 방식으로 재연결한다. 재연결 후 마지막으로 받은 메시지 이후의 메시지를 조회하거나 동기화한다.
- **Heartbeat**: Ping/Pong 또는 애플리케이션 수준의 heartbeat로 끊어진 연결을 감지하고 유령 연결을 정리한다.
- **중복과 순서**: 재전송이 발생할 수 있으므로 `messageId` 또는 `requestId`로 중복을 제거한다. 필요한 경우 서버에서 방별 순번을 부여한다.
- **저장과 전달 순서**: 메시지를 먼저 저장한 뒤 전달하면 재접속 사용자가 메시지를 잃지 않는다. 전달 실패 시에도 저장된 메시지를 기준으로 다시 동기화한다.
- **확장성**: 여러 WebSocket 서버를 운영할 때는 공유 세션 저장소와 Redis Pub/Sub, Redis Streams, Kafka 같은 메시지 브로커를 검토한다.
- **보안과 제한**: 운영 환경에서는 WSS를 사용하고 Origin 검증, 메시지 크기 제한, 전송 빈도 제한, 입력값 검증을 적용한다.

즉, HTTP는 인증·조회·영속 데이터 처리의 기반으로 사용하고, WebSocket은 이미 연결된 사용자에게 새로운 상태 변화를 빠르게 전달하는 통로로 사용하는 것이 실시간 채팅 서비스의 기본 구조다.

## 최종 정리

WebSocket은 한 번 연결하면 계속 연결된 상태를 유지하면서, 클라이언트와 서버가 서로 자유롭게 메시지를 주고받을 수 있는 기술이다.

HTTP는 사용자가 요청해야 서버가 응답하지만, WebSocket은 서버가 먼저 새로운 메시지를 보낼 수 있다. 그래서 실시간 채팅처럼 메시지가 도착하자마자 화면에 보여줘야 하는 서비스에 적합하다.

채팅 서비스에서는 HTTP로 로그인하고 과거 대화 내용을 조회한다. 그다음 WebSocket으로 서버와 연결한 뒤, 새 메시지와 읽음 상태를 실시간으로 주고받는다. 서버는 받은 메시지를 데이터베이스에 저장하고, 같은 대화방에 있는 사람들에게 전달한다.

쉽게 말하면, HTTP는 필요할 때 찾아가는 전화와 같고 WebSocket은 계속 연결해 두는 전화와 같다. 채팅 서비스는 두 기술을 함께 사용해서, 필요한 데이터는 HTTP로 가져오고 실시간 변화는 WebSocket으로 전달한다.
